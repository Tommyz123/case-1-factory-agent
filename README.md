# 工厂搜索智能体 - 案例1

用于查找财富全球500强公司实体制造设施的 AI 智能体。

## 📋 项目概述

**输入**：公司名称（例如："Toyota Motor Corporation"、"Shell"）
**输出**：其实体制造设施的结构化列表

**关键约束**：
- ✅ 必须包含：工厂、车间、炼油厂、组装厂、制造基地
- ❌ 必须排除：总部、办公室、研发中心（包含即自动失败）
- ✅ 必须提供：每个设施的证据URL

## 🏗️ 架构

**框架**：LangGraph（状态管理 + 可观测性）
**LLM**：Azure OpenAI GPT-4.1
**搜索**：多策略（Tavily API > SerpAPI > 网页爬虫）

**工作流**：
```
Input → SearchNode → ExtractionNode → ValidationNode → OutputNode → Result
```

**验证策略**（2层）：
1. **关键词过滤**：快速拒绝 HQ/Office/R&D 关键词
2. **LLM验证**：精确验证边缘案例

## 🚀 快速开始

### 1. 前置要求

- Python 3.8+
- Azure OpenAI API 访问权限

### 2. 安装

```bash
# 克隆或导航到项目目录
cd case-1-factory-agent

# 安装依赖
pip install -r requirements.txt
```

### 3. 配置

**步骤1**：复制环境变量模板
```bash
cp .env.example .env
```

**步骤2**：编辑 `.env` 并填入你的 API 密钥

参考 `case1_config.py` 获取正确的 API 密钥值，并替换下面的占位符：

```bash
# 必需：Azure OpenAI 配置
AZURE_OPENAI_ENDPOINT=<your-azure-endpoint>
AZURE_OPENAI_API_KEY=<your-azure-api-key>
AZURE_OPENAI_API_VERSION=2024-12-01-preview
AZURE_OPENAI_DEPLOYMENT_NAME=<your-gpt-deployment-name>
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=<your-embedding-deployment-name>
AZURE_OPENAI_EMBEDDING_API_VERSION=2024-02-01

# 搜索方法配置（默认：web_scraping，无需 API 密钥）
SEARCH_METHOD=web_scraping

# 日志级别
LOG_LEVEL=INFO
```

**可选**：如果你有 Tavily Search API 密钥：
```bash
SEARCH_METHOD=tavily
TAVILY_API_KEY=<your-tavily-key>
```

### 4. 运行智能体

```bash
python main.py
```

**输入**：`examples/input_example.json`
**输出**：`examples/output_example.json`

## 📁 项目结构

```
case-1-factory-agent/
├── .cache/                 # 运行时缓存（自动生成，已 gitignore）
│   ├── search_results/     # 搜索结果缓存
│   ├── extractions/        # 提取结果缓存
│   └── geocoding/          # 地理编码缓存
├── src/                    # 核心实现
│   ├── __init__.py
│   ├── models.py           # Pydantic 数据模型
│   ├── prompts.py          # LLM 提示模板
│   ├── graph.py            # LangGraph 工作流
│   ├── nodes/              # 处理节点
│   │   ├── __init__.py
│   │   ├── search.py       # 多策略搜索
│   │   ├── extraction.py   # 基于LLM的提取
│   │   ├── validation.py   # 2层验证（关键！）
│   │   ├── geocoding.py    # 地址转坐标
│   │   ├── deduplication.py # 3层去重
│   │   └── output.py       # 输出格式化
│   └── utils/              # 工具模块
│       ├── __init__.py
│       ├── cache.py        # 结果缓存（7天 TTL）
│       ├── metrics.py      # Token/成本追踪
│       ├── facility_classifier.py # 设施类型分类
│       ├── geocoding_service.py   # 地理编码服务
│       └── retry.py        # 重试机制（指数退避）
├── tests/                         # 测试用例
│   ├── conftest.py                # 测试固件（Mock OpenAI 客户端）
│   ├── test_models.py             # 数据模型测试
│   ├── test_validation.py         # 验证测试（关键！HQ/R&D 排除）
│   ├── test_extraction_node.py    # 提取节点测试
│   ├── test_deduplication_node.py # 去重节点测试（3层去重）
│   ├── test_search_node.py        # 搜索节点测试
│   ├── test_geocoding_node.py     # 地理编码节点测试
│   └── test_geocoding_service.py  # 地理编码服务测试
├── docs/
│   └── TECHNICAL_DESIGN.md # 详细技术文档（1900+ 行）
├── examples/
│   ├── input_example.json  # 示例输入
│   └── output_example.json # 示例输出（8个Shell设施）
├── main.py                 # 入口文件
├── requirements.txt        # 依赖包
├── pytest.ini              # 测试配置（覆盖率、格式化）
├── case1_config.py         # API密钥配置模板
├── .env.example            # 环境变量模板
├── .env                    # 实际环境变量文件（已 gitignore）
├── factory_search.log      # 运行时日志（自动生成）
├── READMECase1.md          # 案例要求
└── rubricCase1.md          # 评分标准
```

## 📊 输出格式

```json
{
  "facilities": [
    {
      "company_name": "Toyota Motor Corporation",
      "facility_name": "Toyota Manufacturing Plant",
      "facility_type": "Automotive Assembly Plant",
      "full_address": "123 Factory St, Toyota City, Aichi",
      "country": "Japan",
      "city": "Toyota City",
      "latitude": 35.0833,
      "longitude": 137.1667,
      "evidence_urls": ["https://www.toyota.com/..."],
      "confidence_score": 0.95,
      "validation_passed": true,
      "validation_reason": "Passed keyword and LLM validation"
    }
  ],
  "metadata": {
    "total_facilities": 15,
    "companies_processed": ["Toyota Motor Corporation", "Shell"]
  }
}
```

## ⚠️ 关键验证规则

系统实现了**严格过滤**以避免自动失败：

### 排除关键词（任意匹配 = 拒绝）：
- Headquarters, HQ, Head Office, Corporate Office（总部、总部、总部办公室、企业办公室）
- R&D, Research, Development, Innovation Center（研发、研究、开发、创新中心）
- Sales Office, Regional Office, Administrative（销售办公室、区域办公室、行政办公室）
- Distribution Center, Warehouse, Service Center（配送中心、仓库、服务中心）

### 必需关键词（至少一个）：
- Factory, Plant, Manufacturing, Production（工厂、车间、制造、生产）
- Assembly, Refinery, Mill, Works, Foundry（组装、炼油厂、工厂、工场、铸造厂）

### 2层验证：
1. **第1层（关键词）**：快速过滤，零成本
2. **第2层（LLM）**：精确验证边缘案例

## 🧪 测试

### 自动化测试

运行测试套件：
```bash
# 运行所有测试
pytest -v

# 生成覆盖率报告
pytest --cov=src --cov-report=html

# 运行特定测试文件
pytest tests/test_validation.py -v
```

**关键测试文件**：
- `tests/test_validation.py` - 验证 HQ/R&D/Office 排除（关键！）
- `tests/test_models.py` - 验证 evidence_urls 要求
- `tests/test_extraction_node.py` - 测试 LLM 提取
- `tests/test_deduplication_node.py` - 测试3层去重

### 运行时验证

运行 `python main.py` 后，输出包含自动验证检查：

```
关键验证检查
==========================================
✓ 所有15个设施都有 evidence_urls
✓ 没有设施包含 HQ/R&D/Office 关键词
==========================================
```

### 手动验证
1. 打开 `examples/output_example.json`
2. 随机选择 3-5 个设施
3. 点击它们的 `evidence_urls`
4. 验证它们是真实的制造基地

## 🔍 搜索方法

### 方法1：网页爬虫（默认）
- **优点**：无需 API 密钥，免费
- **缺点**：可能不如 API 可靠
- **配置**：`SEARCH_METHOD=web_scraping`

### 方法2：Tavily Search API（推荐）
- **优点**：AI优化，结构化结果
- **缺点**：需要 API 密钥
- **配置**：
  ```
  SEARCH_METHOD=tavily
  TAVILY_API_KEY=<your-key>
  ```

### 方法3：SerpAPI
- **优点**：可靠的 Google 搜索结果
- **缺点**：需要 API 密钥，需付费
- **配置**：
  ```
  SEARCH_METHOD=serpapi
  SERPAPI_API_KEY=<your-key>
  ```

## 📝 日志

- 控制台：实时进度
- 文件：`factory_search.log`（详细调试信息）

## 🐛 故障排除

### 错误："Missing required environment variables"
**解决方案**：确保你已创建 `.env` 并填入 `case1_config.py` 中的所有 API 密钥

### 错误："Azure OpenAI authentication failed"
**解决方案**：验证你的 `AZURE_OPENAI_API_KEY` 是否正确

### 未找到搜索结果
**解决方案**：
1. 尝试切换搜索方法（例如，从 `web_scraping` 切换到 `tavily`）
2. 检查网络连接
3. 查看 `factory_search.log` 获取详细错误

### 设施被拒绝
**解决方案**：查看 `factory_search.log` - 验证原因已记录：
```
REJECTED (keyword): Toyota HQ - Contains excluded keyword: 'hq'
```

## 📈 性能

**预期性能**（每个公司）：
- 搜索：约 3-5 秒
- 提取：约 5-10 秒（LLM 调用）
- 验证：约 2-5 秒/设施（LLM 调用）
- **总计**：约 20-40 秒/公司

**成本估算**（Azure OpenAI GPT-4.1）：
- 提取：约 2,000 tokens/公司
- 验证：约 100 tokens/设施
- **总计**：$0.01-0.05/公司（假设约10个设施）

## 🎯 成功标准

**及格（75+分）**：
- ✅ 输出中无 HQ/Office/R&D
- ✅ 所有记录都有 evidence_urls
- ✅ 代码运行无错误
- ✅ 提供技术文档

**优秀（85+分）**：
- ✅ 高验证准确率（>90%）
- ✅ 干净的结构化地址
- ✅ 正确的设施类型分类
- ✅ 包含坐标
- ✅ 成本 <$5/公司

## 📚 交付物

### 1. 系统设计文档
**位置**：`docs/TECHNICAL_DESIGN.md`（1,900+ 行，中文）

**内容**：
- 系统概述和约束
- 架构设计及图表
- 数据模型和接口
- 错误处理和日志
- API 集成策略
- 性能优化
- 测试策略

### 2. 核心实现
**入口文件**：`main.py`
**核心代码**：`src/` 目录（12个模块）

**关键特性**：
- 基于 LangGraph 的智能体工作流
- 2层验证（关键词 + LLM）
- 3层去重
- 多策略搜索（Tavily/SerpAPI/网页爬虫）
- 结果缓存和指标追踪

### 3. 测试用例
**位置**：`tests/` 目录（7个测试文件）
**覆盖率**：关键验证逻辑 80%+

**运行测试**：
```bash
pytest -v                          # 所有测试
pytest tests/test_validation.py   # 关键的 HQ/R&D 排除测试
```

### 4. 示例输出
**位置**：`examples/output_example.json`
**内容**：8个 Shell 设施（炼油厂和化工厂）
**验证**：所有设施都有 evidence_urls，无 HQ/Office/R&D 条目

### 5. 时间和Token指标

**实现时间**：约 8 小时
- 初始设置：1 小时
- 核心开发：4 小时
- 测试和验证：2 小时
- 文档编写：1 小时

**Token 使用**（每个公司，估算）：
- 搜索查询：约 500 tokens
- 提取：约 2,000 tokens
- 验证：约 100 tokens × 设施数
- **总计**：约 3,000-5,000 tokens/公司

**成本估算**（Azure OpenAI GPT-4.1）：
- 输入：$0.03/1K tokens
- 输出：$0.06/1K tokens
- **平均**：$0.15-0.25/公司

**指标追踪**：所有运行都会记录 token 使用和成本到：
- 控制台输出（摘要）
- `output_example.json` 元数据部分
- `factory_search.log`（详细）

## 📚 技术细节

**数据模型**：`src/models.py`
- Pydantic 验证确保 evidence_urls 非空
- 所有数据结构的类型安全

**LLM 提示**：`src/prompts.py`
- 提取提示：严格的 JSON schema 输出
- 验证提示：二元 YES/NO 决策

**工作流**：`src/graph.py`
- LangGraph StateGraph 提供可观测性
- 线性流程：搜索 → 提取 → 验证 → 地理编码 → 去重 → 输出

## 🔒 安全注意事项

- `.env` 中的 API 密钥已添加到 gitignore（永不提交！）
- 使用环境变量存储所有机密信息
- LLM temperature=0 保证确定性输出

## 📞 支持

如有问题，请查看：
1. `factory_search.log` 获取详细错误信息
2. `case1_config.py` 查看 API 密钥配置
3. `rubricCase1.md` 查看评分标准

## 📄 许可证

这是一个用于 AI 智能体开发评估的案例研究项目。

---

**最后更新**：2026-01-08
**版本**：1.0.0 (MVP)
